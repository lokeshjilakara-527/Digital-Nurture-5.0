server:
  port: 8082

spring:
  application:
    name: loan-service         # becomes LOAN-SERVICE in Eureka

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
