server:
  port: 8761              # standard Eureka port; dashboard at http://localhost:8761

spring:
  application:
    name: eureka-server

eureka:
  client:
    # This IS the registry, so it should not register with or fetch from itself.
    register-with-eureka: false
    fetch-registry: false
  instance:
    hostname: localhost
